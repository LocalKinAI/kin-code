Refactor this code with these goals:
- Reduce complexity (fewer nested ifs, shorter functions)
- Improve naming (clear, descriptive, consistent)
- Extract reusable parts (DRY, but only if 3+ uses)
- Keep the same behavior (no feature changes)
Show before/after for each change.
